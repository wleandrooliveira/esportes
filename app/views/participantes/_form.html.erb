<%= form_with(model: participante, local: true) do |form| %>
  <% if participante.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(participante.errors.count, "error") %> prohibited this participante from being saved:</h2>

      <ul>
      <% participante.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
  <%= form.label :user_id %>
  <%= form.collection_select :user_id, User.order(:name), :id, :name, :prompt => "Selecione um participante!" %>
  </div>

  <div class="field">
  <%= form.label :partida_id %>
  <%= form.collection_select :partida_id, Partida.order(:title), :title, :title, :prompt => "Selecione uma partida!" %>
  </div>

  <div class="field">
  <%= form.label :partida_id %>
  <%= form.collection_select :partida_id, Partida.order(:local), :local, :local, :prompt => "Selecione o local!" %>
  </div>

  <div class="field">
  <%= form.label :partida_id %>
  <%= form.collection_select :partida_id, Partida.order(:horario), :horario, :horario, :prompt => "Selecione o horario!" %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
